{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mcpaql.org/schemas/danger-level.schema.json",
  "title": "MCP-AQL Danger Level",
  "description": "Schema for operation danger classification in MCP-AQL adapters",
  "$defs": {
    "DangerLevel": {
      "type": "string",
      "title": "Danger Level",
      "description": "Classification of operation risk level. Values: 'safe' (read-only, no side effects), 'reversible' (standard confirmation), 'destructive' (enhanced confirmation), 'dangerous' (explicit unlock required), 'forbidden' (blocked unless admin override)",
      "enum": ["safe", "reversible", "destructive", "dangerous", "forbidden"]
    },
    "DangerClassification": {
      "type": "object",
      "title": "Danger Classification",
      "description": "Complete danger classification for an operation including level and metadata",
      "properties": {
        "level": {
          "$ref": "#/$defs/DangerLevel",
          "description": "The danger level classification for this operation"
        },
        "reason": {
          "type": "string",
          "description": "Human-readable explanation for why this operation has this danger classification"
        },
        "requires_confirmation": {
          "type": "boolean",
          "description": "UI hint indicating whether the operation should prompt for user confirmation before execution",
          "default": false
        },
        "hook_blockable": {
          "type": "boolean",
          "description": "Whether pre-execution hooks can block this operation. When false, hooks receive notification but cannot prevent execution",
          "default": true
        }
      },
      "required": ["level"],
      "additionalProperties": false
    },
    "EndpointDangerDefaults": {
      "type": "object",
      "title": "Endpoint Danger Defaults",
      "description": "Default danger level mappings for CRUDE endpoints",
      "properties": {
        "READ": {
          "const": "safe",
          "description": "READ operations default to safe (read-only, no side effects)"
        },
        "CREATE": {
          "const": "reversible",
          "description": "CREATE operations default to reversible (additive, standard confirmation)"
        },
        "UPDATE": {
          "const": "reversible",
          "description": "UPDATE operations default to reversible (modifying, potentially reversible)"
        },
        "DELETE": {
          "const": "destructive",
          "description": "DELETE operations default to destructive (enhanced confirmation)"
        },
        "EXECUTE": {
          "$ref": "#/$defs/DangerLevel",
          "description": "EXECUTE operations vary from safe to forbidden depending on the specific operation"
        }
      },
      "additionalProperties": false
    }
  },
  "oneOf": [
    {
      "$ref": "#/$defs/DangerLevel"
    },
    {
      "$ref": "#/$defs/DangerClassification"
    }
  ],
  "examples": [
    "safe",
    {
      "level": "safe",
      "reason": "Read-only operation that retrieves data without modification"
    },
    {
      "level": "reversible",
      "reason": "Creates new resource, standard confirmation"
    },
    {
      "level": "destructive",
      "reason": "Permanently removes resource and associated data",
      "requires_confirmation": true,
      "hook_blockable": true
    },
    {
      "level": "forbidden",
      "reason": "Bulk deletion of all user data - catastrophic if executed incorrectly",
      "requires_confirmation": true,
      "hook_blockable": false
    }
  ]
}
